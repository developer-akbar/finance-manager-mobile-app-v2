const fs = require('fs');

function parseCSV(text) {
  if (!text || !text.trim()) return [];
  const src = text.replace(/\r\n/g, '\n').replace(/\r/g, '\n');
  const records = [];
  let fields = [];
  let field = '';
  let inQ = false;
  let i = 0;

  while (i < src.length) {
    const ch = src[i];
    const next = src[i + 1];

    if (inQ) {
      if (ch === '"' && next === '"') {
        field += '"'; i += 2; continue;
      }
      if (ch === '"') {
        inQ = false; i++; continue;
      }
      field += ch; i++; continue;
    }

    if (ch === '"') {
      inQ = true; i++; continue;
    }
    if (ch === ',') {
      fields.push(field); field = ''; i++; continue;
    }
    if (ch === '\n') {
      fields.push(field); field = '';
      records.push(fields); fields = [];
      i++; continue;
    }
    field += ch; i++;
  }
  fields.push(field);
  if (fields.some(f => f !== '')) records.push(fields);

  if (records.length < 2) return [];
  const headers = records[0].map(h => h.trim());
  const rows = [];

  for (let ri = 1; ri < records.length; ri++) {
    const rec = records[ri];
    const row = {};
    headers.forEach((h, idx) => {
      row[h] = (rec[idx] || '').trim();
    });
    if (Object.values(row).every(v => !v)) continue;
    rows.push(row);
  }
  return rows;
}

const normRaw = fs.readFileSync('finman_2026-08-31_Zerodha_final_normalized.csv', 'utf8');
const rows = parseCSV(normRaw);
console.log(`Total rows in normalized CSV: ${rows.length}`);
console.log('Headers:', Object.keys(rows[0]));

// Find RECONCILIATION row in normalized CSV
const reconRows = rows.filter(r => r.InvestmentTransactionType === 'RECONCILIATION' || String(r.Description || '').includes('RECONCILIATION') || String(r.Note || '').includes('Reconciliation'));
console.log(`Reconciliation rows count: ${reconRows.length}`);
reconRows.forEach(r => console.log(JSON.stringify(r, null, 2)));

